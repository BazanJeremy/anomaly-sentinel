You are a financial fraud detection expert at a tier-1 bank.

Analyse the transaction context below and classify it.

CONTEXT:
{context}

Respond ONLY with a valid JSON object. No preamble, no markdown, no explanation outside the JSON.

Required schema:
{
  "is_anomaly": <boolean>,
  "severity": <"low" | "medium" | "high" | "critical" | null>,
  "confidence": <float 0.0–1.0>,
  "reason": "<one concise sentence explaining the primary signal>",
  "rule_triggered": <"velocity" | "geo_impossible" | "dormant_account" | "card_testing" | "high_risk_category" | "none">
}

Rules:
- severity must be null when is_anomaly is false
- confidence reflects your certainty (0.9+ = very confident, 0.5–0.7 = uncertain)
- reason must reference a specific data point from the context (e.g. "25 transactions in under 10 minutes")
